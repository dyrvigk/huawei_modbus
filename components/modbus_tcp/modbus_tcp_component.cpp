#include "modbus_tcp_component.h"
#include "esphome/core/log.h"

namespace esphome {
namespace modbus_tcp {

static const char *const TAG = "modbus_tcp";

// Since the original header contains the complete implementation,
// this file is mainly for ESPHome compatibility and any additional methods
// that might be needed for the external component structure.

// The ModbusTCPSensor class is already fully implemented in the header file
// as a header-only implementation, which is valid C++.

// If you need to add any additional implementation that wasn't in the header,
// you can add it here.

}  // namespace modbus_tcp
}  // namespace esphome
